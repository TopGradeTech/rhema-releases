# Rhema Releases

This repo hosts only signed release builds of [Rhema](https://github.com/TopGradeTelecom/python-translation) — no source code lives here. It exists so the app's in-app "Check for Updates" feature can query GitHub's public Releases API without needing authentication against the private source repository.

Each release attaches `Rhema-Setup.exe`, the Windows installer.
